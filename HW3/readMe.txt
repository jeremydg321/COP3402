/*

Jeremy Galvan
COP3402-20Fall 0001
HW3
*/

1. On the command line, go to the directory of the folder the c file is located in by
entering "cd <folder name>" for ever folder it is within.

2.Once you found the directory, you can enter "gcc compiler.c lex.c parser.c vm.c," which will compile the files, and make an executive file called "a.exe". 


3. Finally, with the input file in the same directory as the files , you can enter "a input.txt -l -a -v", to the file.The arguements "-l," "-a," or "-v" will determine which
file will be echoed in the command line.
	